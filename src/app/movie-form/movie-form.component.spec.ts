import { MovieFormComponent } from './movie-form.component';


describe('addMovie', () => {
    let comp = MovieFormComponent;
    // A jasmine spec
    it('should addMovie', () => {
        // expect(comp.)
    });
  });